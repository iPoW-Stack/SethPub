# FTS 算法：随机性与 BLS 阈值签名

## 核心理念：追随中本聪

Seth 的 FTS (Follow The Satoshi，追随中本聪算法) **借鉴了中本聪在比特币中的思想**：通过引入**不可预测的随机性**来防止系统被操纵。

### 中本聪的随机性

**比特币**：
```
随机性来源 = 工作量证明 (PoW)
- 矿工无法预测下一个区块的哈希值
- 无法提前知道谁会挖到下一个区块
- 防止矿工操纵出块顺序
```

**Seth**：
```
随机性来源 = BLS 阈值签名
- 节点无法预测下一个 epoch 的随机数
- 无法提前知道 FTS 评分的随机部分
- 防止节点操纵评分系统
```

---

## 1. 随机性在 FTS 中的作用

### 1.1 为什么需要随机性？

**没有随机性的问题**：

```cpp
// 假设 FTS 完全确定性
FTS = f(IP, 信用, 质押, 交易数, 共识间隙)

问题：
1. 节点可以精确计算自己的 FTS
2. 节点可以优化参数以刚好避免被淘汰
3. 恶意节点可以"卡线"（刚好在淘汰线之上）
4. 系统可被预测和操纵
```

**引入随机性后**：

```cpp
// FTS 包含随机部分
FTS = f(IP, 信用, 质押, 交易数, 共识间隙, 随机数)

优势：
1. 节点无法精确预测自己的 FTS
2. 无法通过优化参数来保证不被淘汰
3. "卡线"策略失效
4. 系统不可预测，难以操纵
```

### 1.2 随机性的应用位置

从代码中可以看到，随机性主要用于**质押权重的平滑处理**：

```cpp
// src/consensus/zbft/elect_tx_item.cc

// 初始化随机数生成器，种子来自 BLS 阈值签名
g2_ = std::make_shared<std::mt19937_64>(vss_mgr_->EpochRandom());

// 在计算质押权重时使用随机数
for (uint32_t i = 1; i < elect_nodes.size(); ++i) {
    uint64_t fts_val_diff = elect_nodes[i]->stoke - elect_nodes[i - 1]->stoke;
    
    if (fts_val_diff == 0) {
        blance_weight[i] = blance_weight[i - 1];
    } else {
        if (fts_val_diff < diff_2b3) {
            // 关键：使用随机数
            auto rand_val = fts_val_diff + g2() % (diff_2b3 - fts_val_diff);
            blance_weight[i] = blance_weight[i - 1] + (20 * rand_val) / diff_2b3;
        } else {
            // 关键：使用随机数
            auto rand_val = diff_2b3 + g2() % (fts_val_diff + 1 - diff_2b3);
            blance_weight[i] = blance_weight[i - 1] + (20 * rand_val) / fts_val_diff;
        }
    }
}
```

**效果**：
- 相同质押量的节点，每个 epoch 的权重略有不同
- 防止节点通过精确计算来操纵排名
- 增加系统的不可预测性

---

## 2. BLS 阈值签名生成随机数

### 2.1 什么是 BLS 阈值签名？

**BLS (Boneh-Lynn-Shacham) 签名**：
- 一种基于椭圆曲线的数字签名方案
- 支持签名聚合（多个签名合并为一个）
- 支持阈值签名（t-of-n，n个节点中任意t个可以生成有效签名）

**阈值签名的特性**：
```
设置：n = 1024 个节点，t = 683 (2/3)

特性：
1. 任意 683 个节点可以合作生成签名
2. 少于 683 个节点无法生成有效签名
3. 签名结果是确定的（相同输入 → 相同输出）
4. 但在签名生成前，结果不可预测
```

### 2.2 随机数生成流程

```
Step 1: 时间块创建
├─ 每 10 分钟创建一个时间块
├─ 时间块包含时间戳和其他元数据
└─ 时间块需要被 2/3 节点签名确认

Step 2: BLS 阈值签名
├─ 至少 683 个节点参与签名
├─ 每个节点使用自己的私钥片段签名
├─ 签名片段聚合成完整签名
└─ 签名结果是一个 G2 点（椭圆曲线上的点）

Step 3: 提取随机数
├─ 从 BLS 签名中提取数值
├─ 签名是椭圆曲线上的点 (x, y)
├─ 使用点的坐标作为随机数种子
└─ epoch_random = hash(BLS_signature)

Step 4: 初始化随机数生成器
├─ 使用 epoch_random 作为种子
├─ 初始化 Mersenne Twister (mt19937_64)
└─ 在 FTS 计算中使用这个生成器
```

### 2.3 代码实现

```cpp
// 1. VSS Manager 存储 epoch 随机数
class VssManager {
public:
    uint64_t EpochRandom() {
        return epoch_random_;
    }
    
private:
    std::atomic<uint64_t> epoch_random_{ 0 };
};

// 2. 在选举交易中使用随机数
ElectTxItem::HandleTx(...) {
    // 使用 BLS 生成的随机数初始化随机数生成器
    g2_ = std::make_shared<std::mt19937_64>(vss_mgr_->EpochRandom());
    
    // 后续在 FTS 计算中使用 g2_()
}

// 3. 在质押权重计算中使用
SmoothFtsValue(...) {
    auto& g2 = *g2_;  // 获取随机数生成器
    
    for (uint32_t i = 1; i < elect_nodes.size(); ++i) {
        // 使用随机数平滑质押权重
        auto rand_val = fts_val_diff + g2() % (diff_2b3 - fts_val_diff);
        blance_weight[i] = blance_weight[i - 1] + (20 * rand_val) / diff_2b3;
    }
}
```

---

## 3. 安全性分析

### 3.1 为什么 BLS 阈值签名安全？

**1. 去中心化生成**
```
随机数不是由单个节点生成
而是由 2/3 节点（683个）共同生成
任何单个节点都无法控制结果
```

**2. 不可预测性**
```
在签名生成前：
- 没有节点知道最终的签名值
- 没有节点知道随机数是什么
- 无法提前计算 FTS 评分

在签名生成后：
- 随机数确定且不可更改
- 所有节点使用相同的随机数
- 确保 FTS 计算的一致性
```

**3. 不可操纵性**
```
要操纵随机数，需要：
- 控制至少 683 个节点（2/3）
- 这些节点必须在签名时协同作恶
- 攻击成本极高（见下文）
```

**4. 可验证性**
```
BLS 签名可以被验证：
- 任何人都可以验证签名的有效性
- 确保随机数是正确生成的
- 防止伪造随机数
```

### 3.2 攻击场景分析

#### 场景 1：单个节点试图操纵

**攻击方式**：
```
恶意节点想要操纵随机数以提高自己的 FTS
```

**防御**：
```
失败原因：
1. 单个节点只有 1/1024 的签名份额
2. 需要 683 个节点才能生成签名
3. 单个节点无法影响最终结果

结论：攻击失败
```

#### 场景 2：少数节点联合操纵

**攻击方式**：
```
100 个恶意节点联合，试图操纵随机数
```

**防御**：
```
失败原因：
1. 100 < 683，无法生成有效签名
2. 即使参与签名，也只占 100/683 = 14.6%
3. 无法控制最终的签名结果

结论：攻击失败
```

#### 场景 3：多数节点联合操纵

**攻击方式**：
```
683 个恶意节点联合，试图操纵随机数
```

**防御**：
```
理论上可行，但：
1. 需要控制 683 个节点
2. 质押成本：683 × 256 = 174,848 SETH
3. 硬件成本：683 × $2000 = $1,366,000
4. 需要这些节点在 FTS 评分中都足够高（不被淘汰）
5. 即使成功，也只能影响一个 epoch
6. 下一个 epoch 会重新生成随机数
7. 恶意行为会被记录，信用永久受损

攻击成本：174,848 SETH + $1.37M + 长期信用损失
攻击收益：影响一个 epoch 的 FTS 评分

结论：攻击成本远大于收益
```

### 3.3 与中本聪共识的对比

| 维度 | 比特币 (PoW) | Seth (BLS + FTS) |
|------|-------------|------------------|
| **随机性来源** | 哈希碰撞 | BLS 阈值签名 |
| **生成方式** | 单个矿工 | 2/3 节点协作 |
| **可预测性** | 不可预测 | 不可预测 |
| **可验证性** | 可验证 | 可验证 |
| **能源消耗** | 极高 | 极低 |
| **去中心化** | 高 | 极高 |
| **攻击成本** | 51% 算力 | 2/3 节点 + 质押 |

**相似之处**：
- 都使用随机性防止操纵
- 都不可预测
- 都可验证

**Seth 的优势**：
- 能源效率高（不需要 PoW）
- 更去中心化（百万级节点 vs 几千矿池）
- 攻击成本更高（需要质押 + 多维度表现）

---

## 4. 随机性的数学原理

### 4.1 BLS 签名的数学基础

**椭圆曲线**：
```
使用 alt_bn128 曲线（以太坊也使用）
点的形式：G2 = (x, y, z) 其中 x, y, z 是复数
```

**签名生成**：
```
每个节点 i 有私钥片段 sk_i
签名片段：sig_i = sk_i * H(message)
聚合签名：sig = Σ sig_i (至少 t 个)
```

**随机数提取**：
```
epoch_random = hash(sig.x || sig.y || sig.z)
```

### 4.2 随机性的统计特性

**均匀分布**：
```
BLS 签名的输出在椭圆曲线上均匀分布
提取的随机数在 [0, 2^64) 范围内均匀分布
```

**独立性**：
```
每个 epoch 的随机数独立
无法从前一个 epoch 的随机数预测下一个
```

**确定性**：
```
相同的输入（时间块）→ 相同的签名 → 相同的随机数
确保所有节点计算的 FTS 一致
```

### 4.3 Mersenne Twister 的作用

**为什么使用 MT19937**：
```cpp
g2_ = std::make_shared<std::mt19937_64>(vss_mgr_->EpochRandom());
```

**原因**：
1. **高质量随机数**：MT19937 是工业标准的伪随机数生成器
2. **长周期**：周期为 2^19937-1，远大于需求
3. **确定性**：相同种子 → 相同序列
4. **高效**：生成速度快

**使用方式**：
```cpp
// 种子来自 BLS 签名
uint64_t seed = vss_mgr_->EpochRandom();

// 初始化生成器
std::mt19937_64 g2(seed);

// 生成随机数
uint64_t rand1 = g2();  // 第一个随机数
uint64_t rand2 = g2();  // 第二个随机数
// ...
```

---

## 5. 实际效果演示

### 5.1 没有随机性的 FTS

**场景**：两个节点质押量相同

```
节点 A：质押 256 SETH
节点 B：质押 256 SETH

没有随机性：
- 质押权重 A = 100
- 质押权重 B = 100
- 完全相同，可预测

问题：
- 节点可以精确计算排名
- 可以通过微调其他参数来操纵排名
```

### 5.2 有随机性的 FTS

**场景**：两个节点质押量相同

```
节点 A：质押 256 SETH
节点 B：质押 256 SETH

Epoch 1（随机数 = 12345）：
- 质押权重 A = 100 + rand(0, 20) = 112
- 质押权重 B = 100 + rand(0, 20) = 107
- A 略高于 B

Epoch 2（随机数 = 67890）：
- 质押权重 A = 100 + rand(0, 20) = 105
- 质押权重 B = 100 + rand(0, 20) = 118
- B 高于 A

效果：
- 节点无法精确预测排名
- 无法通过微调来保证不被淘汰
- 必须在所有维度都表现优秀
```

### 5.3 随机性对淘汰的影响

**场景**：节点在淘汰边缘

```
分片有 1024 个节点
淘汰最低 10% = 102 个节点

节点 X 的 FTS（不含随机性）：
- IP权重：120
- 信用权重：130
- 质押权重：100（确定）
- 交易数权重：110
- 共识间隙权重：115
- 总 FTS = 115（排名第 920）

没有随机性：
- 节点 X 知道自己排名 920
- 安全（不在最低 102 名）
- 可以"躺平"

有随机性：
- 质押权重变为 100 + rand(0, 20)
- 可能是 105，也可能是 118
- 总 FTS 可能是 110-125
- 排名可能是 900-950
- 有可能进入最低 102 名
- 不能"躺平"，必须持续优化
```

---

## 6. 总结：FTS 的随机性设计

### 6.1 设计理念

**借鉴中本聪**：
```
比特币：通过 PoW 的随机性防止操纵
Seth：通过 BLS 阈值签名的随机性防止操纵
```

**核心思想**：
```
不可预测性 + 可验证性 + 去中心化生成 = 安全的随机性
```

### 6.2 技术实现

```
BLS 阈值签名
    ↓
提取随机数种子
    ↓
初始化 MT19937
    ↓
在 FTS 计算中使用
    ↓
防止系统被操纵
```

### 6.3 安全保证

1. **去中心化**：2/3 节点共同生成
2. **不可预测**：签名前无法知道结果
3. **不可操纵**：需要控制 683 个节点
4. **可验证**：BLS 签名可以验证
5. **确定性**：相同输入 → 相同输出

### 6.4 与传统 PoS 的区别

**传统 PoS**：
```
FTS = f(质押量)  // 完全确定
节点可以精确计算排名
可以通过质押量操纵
```

**Seth FTS**：
```
FTS = f(5维度, 随机数)  // 部分随机
节点无法精确预测排名
难以通过单一维度操纵
```

### 6.5 最终效果

**防止操纵**：
- 节点无法精确预测 FTS
- 无法通过"卡线"策略避免淘汰
- 必须在所有维度都表现优秀

**保持公平**：
- 随机性只影响质押权重（20%）
- 其他 80% 仍基于实际表现
- 优秀节点仍然有优势

**增加安全**：
- 攻击者无法预测系统行为
- 难以通过操纵参数来攻击
- 系统更加健壮

---

**Seth FTS 的随机性设计**：

> "借鉴中本聪的智慧，  
> 使用 BLS 阈值签名生成不可预测的随机数，  
> 在保持公平的同时，防止系统被操纵。"

**这是一个结合了密码学、博弈论和经济学的精妙设计。**
