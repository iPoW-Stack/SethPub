# Epoch 周期更新说明

## 变更内容

### 原始配置
```cpp
static const int64_t kRotationPeriod = 120ll * 1000ll * 1000ll;  // 120秒
static const uint32_t kHalvingPeriodEpochs = 1051200u;  // 基于120秒计算
```

### 新配置
```cpp
static const int64_t kRotationPeriod = 600ll * 1000ll * 1000ll;  // 600秒 (10分钟)
static const uint32_t kHalvingPeriodEpochs = 210240u;  // 基于600秒重新计算
```

## 计算说明

### Epoch 周期
- **旧值**：120 秒 (2 分钟)
- **新值**：600 秒 (10 分钟)
- **变化**：增加 5 倍

### 减半周期计算

#### 旧计算（120秒 epoch）
```
4年 = 365.25 天 × 24 小时 × 3600 秒 / 120 秒/epoch × 4 年
    = 31,557,600 秒/年 / 120 秒/epoch × 4 年
    = 262,980 epochs/年 × 4 年
    = 1,051,200 epochs
```

#### 新计算（600秒 epoch）
```
4年 = 365.25 天 × 24 小时 × 3600 秒 / 600 秒/epoch × 4 年
    = 31,557,600 秒/年 / 600 秒/epoch × 4 年
    = 52,596 epochs/年 × 4 年
    = 210,240 epochs
```

## 影响分析

### 1. 减半时间表

| 减半次数 | Epoch 范围 | 实际时间 | 每 Epoch 奖励 |
|---------|-----------|---------|--------------|
| 0 | 0 - 210,239 | 0 - 4年 | 10,000 SETH |
| 1 | 210,240 - 420,479 | 4 - 8年 | 5,000 SETH |
| 2 | 420,480 - 630,719 | 8 - 12年 | 2,500 SETH |
| 3 | 630,720 - 840,959 | 12 - 16年 | 1,250 SETH |
| 4 | 840,960 - 1,051,199 | 16 - 20年 | 625 SETH |

### 2. 年产出对比

#### 旧配置（120秒 epoch）
```
每年 epochs: 262,980
第1年产出: 262,980 × 10,000 = 2,629,800,000 SETH
```

#### 新配置（600秒 epoch）
```
每年 epochs: 52,596
第1年产出: 52,596 × 10,000 = 525,960,000 SETH
```

**变化**：年产出减少到原来的 **1/5**（因为 epoch 周期增加了 5 倍）

### 3. 通胀率对比

假设初始流通量为 210 亿 SETH：

| 配置 | 年产出 | 通胀率 |
|------|--------|--------|
| 旧配置 (120s) | 2.63B SETH | 0.0125% |
| 新配置 (600s) | 526M SETH | 0.0025% |

**变化**：通胀率降低到原来的 **1/5**

### 4. 分片奖励示例

#### 初始阶段（3个分片）

**旧配置**：
- 每分片每 epoch：~4,155 SETH
- 每分片每天：~4,155 × 720 = 2,991,600 SETH

**新配置**：
- 每分片每 epoch：~4,155 SETH（不变）
- 每分片每天：~4,155 × 144 = 598,320 SETH

**变化**：每天奖励减少到原来的 **1/5**

## 优势分析

### 1. 更低的通胀率
- 年通胀率从 0.0125% 降至 0.0025%
- 更有利于代币价值稳定

### 2. 更长的挖矿周期
- 相同的减半次数下，挖矿时间更长
- 给予网络更多时间成长

### 3. 更合理的 Epoch 周期
- 10 分钟的 epoch 周期更适合共识
- 减少频繁的选举和轮换开销

### 4. 供应量更可控
- 100 年累计产出：~105M SETH（旧配置：~525M SETH）
- 占总供应量比例更小

## 代码变更

### 修改的文件
- `src/common/utils.h`

### 修改的常量
```cpp
// 从
static const uint32_t kHalvingPeriodEpochs = 1051200u;

// 改为
static const uint32_t kHalvingPeriodEpochs = 210240u;
```

### 计算公式（保持不变）
```cpp
halving_count = epoch_number / kHalvingPeriodEpochs
base_reward = kInitialTotalReward / (2^halving_count)
```

## 验证

### 编译检查
```bash
✓ src/common/utils.h - 无错误
✓ src/consensus/zbft/elect_tx_item.cc - 无错误
✓ src/consensus/zbft/elect_tx_item.h - 无错误
```

### 数学验证
```
Epoch 0: 10,000 SETH
Epoch 210,240: 5,000 SETH (4年后，第一次减半) ✓
Epoch 420,480: 2,500 SETH (8年后，第二次减半) ✓
```

### 时间验证
```
210,240 epochs × 600 秒/epoch = 126,144,000 秒
126,144,000 秒 / (365.25 × 24 × 3600) = 4.0 年 ✓
```

## 更新的文档

需要更新以下文档中的数值：

1. **DYNAMIC_SHARDING_REWARD_DESIGN.md**
   - 减半周期：1,051,200 → 210,240
   - 年产出：2.628B → 525.96M
   - 通胀率：0.0125% → 0.0025%

2. **DYNAMIC_SHARDING_QUICK_REF.md**
   - 减半周期参数
   - 典型场景计算

3. **ECONOMIC_MODEL_IMPLEMENTATION.md**
   - 所有涉及 epoch 数量的计算

## 总结

| 指标 | 旧值 (120s) | 新值 (600s) | 变化 |
|------|------------|------------|------|
| Epoch 周期 | 120秒 | 600秒 | ×5 |
| 减半周期 | 1,051,200 epochs | 210,240 epochs | ÷5 |
| 年产出 | 2.63B SETH | 526M SETH | ÷5 |
| 通胀率 | 0.0125% | 0.0025% | ÷5 |
| 每天 epochs | 720 | 144 | ÷5 |

**核心影响**：
- ✅ 通胀率大幅降低（更健康）
- ✅ 供应量更可控
- ✅ Epoch 周期更合理
- ✅ 代码逻辑无需改动（只改常量）

---

**更新日期**：2026-04-15  
**版本**：v2.1 - Epoch Period Update
